VITE_DEV_STAGE=true
VITE_API_BASE_URL=https://api.alkachof.mx
# Public app root used to build shareable catalog links. Placeholder for now.
VITE_PUBLIC_APP_URL=https://app.alkachof.mx
# Phyllo Connect environment for the Instagram import. MUST match the API's
# PHYLLO_ENV — a sandbox client cannot open a token minted for production.
VITE_PHYLLO_ENV=sandbox

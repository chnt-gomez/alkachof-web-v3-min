## Define a theme for Alkachof

Alkachof UI is a shell suited for quick tests and evaluation. We now need to set the alpha version so its now focused on a final look and feel. The UI principles are detailes in CLAUDE.md.

# What to do next:

We want to make the page looks and feels like a mobile app. Use the current barebones of the application to start and use it as a guide to overhaul the application so it looks like a finished app instead of a simple prototype.
# Copy to .env.local to override a stage locally. Vite only exposes vars
# prefixed with VITE_ to the client bundle.

# true  -> every action returns mock data, no HTTP at all (see src/lib/stage.ts)
# false -> real backend
VITE_DEV_STAGE=false

# API origin. Also the Socket.IO host for the /live namespace.
# Point at http://localhost:3001 to run against a local alkachof-api.
VITE_API_BASE_URL=https://api.alkachof.mx

# Public app root, used to build shareable catalog/product links.
VITE_PUBLIC_APP_URL=https://app.alkachof.mx

# Phyllo Connect environment for the Instagram import. MUST match the API's
# PHYLLO_ENV. Anything other than "production" uses Phyllo's sandbox.
VITE_PHYLLO_ENV=sandbox
